# =============================================================================
# 5G Telecom AI Engine — Requirements
# =============================================================================
# Split into sections: core training, inference, RAG, API, and utils.
# Pin versions for reproducibility; tested on Python 3.10+

# --- Core ML / Training ---
torch>=2.1.0                    # PyTorch — base DL framework
transformers>=4.40.0            # HuggingFace model hub + training utilities
datasets>=2.18.0                # HuggingFace datasets loader
peft>=0.10.0                    # Parameter-Efficient Fine-Tuning (LoRA/QLoRA)
bitsandbytes>=0.43.0            # 4-bit / 8-bit quantization (QLoRA)
trl>=0.8.6                      # SFTTrainer for instruction fine-tuning
accelerate>=0.28.0              # Multi-GPU / mixed-precision training
einops>=0.7.0                   # Tensor rearrangement (required by some models)

# --- RAG / Vector Store ---
faiss-cpu>=1.7.4                # Facebook AI Similarity Search (CPU build)
# faiss-gpu>=1.7.4              # Uncomment if CUDA available
chromadb>=0.4.24                # Alternative vector DB (persistent, simple API)
sentence-transformers>=2.6.1    # Embedding models for RAG retrieval
langchain>=0.1.16               # Orchestration: retrieval + LLM chaining
langchain-community>=0.0.36     # Community loaders (PDF, text, etc.)

# --- API / Serving ---
fastapi>=0.110.0                # REST API framework
uvicorn[standard]>=0.29.0       # ASGI server for FastAPI
pydantic>=2.6.0                 # Request/response schema validation

# --- Data Processing / Utils ---
pandas>=2.2.0                   # Tabular data manipulation
numpy>=1.26.0                   # Numerical operations
jsonschema>=4.21.0              # JSON schema validation for datasets
rich>=13.7.0                    # Beautiful CLI output (tables, progress bars)
click>=8.1.7                    # CLI argument parsing

# --- Evaluation ---
rouge-score>=0.1.2              # ROUGE metric for text quality
nltk>=3.8.1                     # NLP utilities (tokenization, BLEU)
evaluate>=0.4.1                 # HuggingFace evaluation library

# --- Notebook / Colab ---
ipywidgets>=8.1.2               # Progress widgets in Jupyter/Colab
tqdm>=4.66.2                    # Progress bars in scripts

# --- Dev / Testing ---
pytest>=8.1.0                   # Unit testing
black>=24.3.0                   # Code formatting
isort>=5.13.2                   # Import sorting
